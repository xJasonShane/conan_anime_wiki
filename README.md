# 名侦探柯南 Wiki

基于 VitePress 构建的《名侦探柯南》电视动画作品资料库网站。

## 功能特性

- 📊 **数据统计**: 剧集总数、年度分布等统计信息
- 📝 **剧集列表**: 完整的剧集列表,支持排序和分页
- 🔍 **搜索功能**: 快速搜索剧集号、标题等信息
- 📱 **响应式设计**: 完美适配桌面和移动设备

## 技术栈

- **框架**: VitePress
- **语言**: JavaScript (Vue 3)
- **样式**: CSS (Scoped Styles)
- **数据**: JSON

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run docs:dev
```

访问 http://localhost:5173 查看网站

### 构建生产版本

```bash
npm run docs:build
```

### 预览生产版本

```bash
npm run docs:preview
```

## 项目结构

```
conan_anime_wiki/
├── docs/
│   ├── .vitepress/
│   │   └── config.mjs          # VitePress 配置
│   ├── data/
│   │   ├── episodes.json       # 剧集数据
│   │   └── utils.js            # 数据处理工具函数
│   ├── index.md                # 首页
│   ├── episodes.md             # 剧集列表页
│   ├── search.md               # 搜索页
│   └── about.md                # 关于页
└── package.json
```

## 数据格式

剧集数据存储在 `docs/data/episodes.json` 中,格式如下:

```json
{
  "episodes": [
    {
      "id": 1,
      "cnTitle": "云霄飞车杀人事件",
      "jpTitle": "ジェットコースター殺人事件",
      "airDate": "1996-01-08",
      "manga": "File.1-File.2",
      "notes": "动画第一集"
    }
  ]
}
```

## 开发说明

### 添加新剧集

编辑 `docs/data/episodes.json` 文件,添加新的剧集数据。

### 自定义样式

各页面使用 scoped CSS 样式,可以直接在对应的 `.md` 文件中修改 `<style scoped>` 部分。

### 修改配置

编辑 `docs/.vitepress/config.mjs` 文件可以修改网站配置,包括导航、侧边栏、主题等。

## 许可证

MIT License
